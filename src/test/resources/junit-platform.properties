# Feature files run only through CucumberTest (a JUnit Platform suite), never twice.
cucumber.junit-platform.discovery.as-root-engine=false
# Scenarios share one application context and reset it between them, so they must not run in parallel.
cucumber.execution.parallel.enabled=false
cucumber.plugin=pretty
cucumber.publish.quiet=true
